# Local dev environment (laptop). NOT for the HPC box — the HPC stack lives in requirements.txt.
# Install with: uv venv --python 3.13 .venv && uv pip install -r requirements-dev.txt --index-strategy unsafe-best-match
# Python 3.13; CPU-only torch (no CUDA wheels). Versions mirror requirements.txt where they
# support Python 3.13; numba is left to the resolver because the pinned 0.61.2 tops out at 3.12.

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.9.1+cpu
torchvision==0.24.1+cpu

numpy==2.2.6
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0
numba
matplotlib==3.10.9
pydantic==2.12.5
requests==2.32.5
tqdm==4.67.3
python-dotenv==1.2.2

# Alerts extraction (PDF OCR + Gemini). Heavy; omit if not needed.
docling==2.76.0
google-genai==1.66.0
